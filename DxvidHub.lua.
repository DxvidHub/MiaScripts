-- DXV1D | VERSIÓN GITHUB OFICIAL V2 | CORRECIÓN RE-ACTIVACIÓN SPOOFER
local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui")
gui.Name = "DXV1DRainbow"
gui.Parent = player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

-- VARIABLES DE ESTADO
local espEnabled = false
local abierto = false
local spoofEnabled = false 

-- CONFIGURACIÓN DEL SPOOFER
local TagColor = "#00FFFF" 
local NombreMOD = '<font color="'..TagColor..'">[MOD]</font> DXV1D'
local NombrePlano = "[MOD] DXV1D"
local NombreReal = player.Name
local DisplayReal = player.DisplayName

-- ==========================================
-- 1. BOTÓN PRINCIPAL Y ARRASTRE (SIN CAMBIOS)
-- ==========================================
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 150, 0, 38)
mainFrame.Position = UDim2.new(0.5, -75, 0.1, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
mainFrame.BackgroundTransparency = 0.3
mainFrame.Active = true
mainFrame.Parent = gui

local strokeMain = Instance.new("UIStroke", mainFrame)
strokeMain.Thickness = 2
strokeMain.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local corner = Instance.new("UICorner", mainFrame)
corner.CornerRadius = UDim.new(0, 10)

local titleBtn = Instance.new("TextButton")
titleBtn.Size = UDim2.new(1, 0, 1, 0)
titleBtn.BackgroundTransparency = 1
titleBtn.Text = "DXV1D"
titleBtn.Font = Enum.Font.GothamBold
titleBtn.TextSize = 18
titleBtn.Parent = mainFrame

local UserInputService = game:GetService("UserInputService")
local dragging, dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

titleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)

titleBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then update(input) end
end)

-- ==========================================
-- 2. PANEL DE OPCIONES
-- ==========================================
local optionsFrame = Instance.new("Frame")
optionsFrame.Size = UDim2.new(1, 0, 0, 175)
optionsFrame.Position = UDim2.new(0, 0, 1, 5)
optionsFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
optionsFrame.BackgroundTransparency = 0.4
optionsFrame.Visible = false
optionsFrame.Parent = mainFrame
Instance.new("UICorner", optionsFrame)

local strokeOptions = Instance.new("UIStroke", optionsFrame)
strokeOptions.Thickness = 2
strokeOptions.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local layout = Instance.new("UIListLayout", optionsFrame)
layout.Padding = UDim.new(0, 5)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function crearBoton(texto)
    local btn = Instance.new("TextButton", optionsFrame)
    btn.Size = UDim2.new(0.9, 0, 0, 30)
    btn.Text = texto
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    Instance.new("UICorner", btn)
    return btn
end

local speedInput = Instance.new("TextBox", optionsFrame)
speedInput.Size = UDim2.new(0.9, 0, 0, 30)
speedInput.PlaceholderText = "Velocidad"
speedInput.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
speedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
speedInput.Font = Enum.Font.GothamBold
speedInput.TextSize = 14
Instance.new("UICorner", speedInput)

local btnEsp = crearBoton("ESP: OFF")
local btnSpoof = crearBoton("Mod Spoofer: OFF")
local btnSilent = crearBoton("Silent Aim")

-- FUNCION DE REEMPLAZO (SPOOFER)
local function AplicarEstiloReal(instancia)
    if spoofEnabled and (instancia:IsA("TextLabel") or instancia:IsA("TextButton")) then
        local texto = instancia.Text
        if (string.find(texto, NombreReal) or string.find(texto, DisplayReal)) and not string.find(texto, "[MOD]") then
            instancia.RichText = true 
            instancia.Text = NombreMOD
        end
    end
end

-- LÓGICA DE BOTONES
btnEsp.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    btnEsp.Text = espEnabled and "ESP: ON" or "ESP: OFF"
    btnEsp.TextColor3 = espEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 255, 255)
    if not espEnabled then
        for _, p in pairs(game.Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("DXV1D_HL") then
                p.Character.DXV1D_HL:Destroy()
            end
        end
    end
end)

btnSpoof.MouseButton1Click:Connect(function()
    spoofEnabled = not spoofEnabled
    btnSpoof.Text = spoofEnabled and "Mod Spoofer: ON" or "Mod Spoofer: OFF"
    btnSpoof.TextColor3 = spoofEnabled and Color3.fromRGB(0, 255, 255) or Color3.fromRGB(255, 255, 255)
    
    if spoofEnabled then
        -- RE-APLICACIÓN INMEDIATA AL ACTIVAR
        pcall(function()
            player.DisplayName = NombrePlano
            if player.Character then
                if player.Character:FindFirstChildOfClass("Humanoid") then
                    player.Character:FindFirstChildOfClass("Humanoid").DisplayName = NombrePlano
                end
                for _, v in ipairs(player.Character:GetDescendants()) do
                    if v:IsA("TextLabel") then AplicarEstiloReal(v) end
                end
            end
        end)
    else
        pcall(function() player.DisplayName = DisplayReal end)
    end
end)

btnSilent.MouseButton1Click:Connect(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/DxvidHub/MiaScripts/main/Misilent.lua"))()
    end)
    btnSilent.TextColor3 = Color3.fromRGB(0, 255, 255)
end)

speedInput.FocusLost:Connect(function(enter)
    if enter then
        local s = tonumber(speedInput.Text)
        if s and player.Character then player.Character.Humanoid.WalkSpeed = s end
    end
end)

titleBtn.MouseButton1Up:Connect(function()
    if not dragging then
        abierto = not abierto
        optionsFrame.Visible = abierto
    end
end)

-- ==========================================
-- 3. BUCLE MAESTRO (RGB + ESP + SPOOFER)
-- ==========================================
spawn(function()
    local hue = 0
    while true do
        hue = hue + 1/255
        if hue > 1 then hue = 0 end
        local color = Color3.fromHSV(hue, 1, 1)
        
        -- RGB Sync
        titleBtn.TextColor3 = color
        strokeMain.Color = color
        strokeOptions.Color = color
        
        -- ESP Logic
        if espEnabled then
            for _, p in pairs(game.Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hl = p.Character:FindFirstChild("DXV1D_HL") or Instance.new("Highlight")
                    hl.Name = "DXV1D_HL"
                    hl.Parent = p.Character
                    hl.FillColor = Color3.fromRGB(255, 0, 0)
                    hl.Enabled = true
                end
            end
        end

        -- Spoofer Logic (Bucle de mantenimiento)
        if spoofEnabled then
            pcall(function()
                player.DisplayName = NombrePlano
                if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
                    player.Character:FindFirstChildOfClass("Humanoid").DisplayName = NombrePlano
                end
                for _, v in ipairs(player.PlayerGui:GetDescendants()) do 
                    if v:IsA("TextLabel") then AplicarEstiloReal(v) end 
                end
                for _, v in ipairs(game:GetService("CoreGui"):GetDescendants()) do 
                    if v:IsA("TextLabel") then AplicarEstiloReal(v) end 
                end
            end)
        end
        
        task.wait(0.3)
    end
end)

game.DescendantAdded:Connect(function(obj)
    if spoofEnabled and (obj:IsA("TextLabel") or obj:IsA("TextButton")) then
        task.defer(AplicarEstiloReal, obj)
    end
end)
